# SmartClock 测试计划

更新时间：2026-06-06

## 说明
- 目标：把当前版本的构建稳定性、页面导航、提醒功能、真机链路和待优化项拉成一张持续执行的清单。
- 状态说明：
  - `未测`：尚未开始
  - `进行中`：正在执行
  - `通过`：已验证通过
  - `失败`：已复现问题，待修复
  - `待人工`：当前轮次不适合自动化，需要手工或重启设备配合

## 测试清单
| ID | 模块 | 测试内容 | 测试方式 | 状态 | 结果/备注 | 后续优化 |
|---|---|---|---|---|---|---|
| T01 | 构建 | `assembleDebug` + `testDebugUnitTest` | Gradle | 通过 | 2026-06-06 顺序执行通过 | 保持单进程顺序构建 |
| T02 | 启动 | App 冷启动、首页首屏渲染 | 真机启动 | 通过 | `MainActivity` 拉起成功，无崩溃 | 后续可补首屏耗时统计 |
| T03 | 导航 | 底部 `提醒 / 日历 / 模板 / 设置` 四入口切换 | 真机点击 + `uiautomator`/截图 | 通过 | 四页截图已保存，`logcat` 无 `AndroidRuntime` 崩溃 | 防止后续改版再引入切页闪退 |
| T04 | 提醒页 | 顶部文案、筛选按钮单行显示、卡片时间完整显示 | 真机截图 | 通过 | 首页提醒页截图确认当前布局符合最新要求 | 继续压缩信息密度 |
| T05 | 提醒页交互 | 左右滑动删除的跟手性、吸附、删除按钮视觉 | 真机操作 + 截图 | 通过 | 滑动删除可正常露出侧边删除区，截图已保存 | 继续收口阻尼和阈值反馈 |
| T06 | 日历页 | 月/周/日视图打开与切换，无闪退 | 真机点击 + 截图 | 通过 | 月/周/日三视图都能打开并截图，无闪退 | 检查切换动画和密度 |
| T07 | 模板页 | 8 个模板页面渲染、入口点击 | 真机导航 | 通过 | 模板页可正常进入，首屏渲染正常 | 统一模板卡片信息层级 |
| T08 | 设置页 | 页面渲染、权限状态刷新、引导卡片可见 | 真机导航 | 通过 | 设置页可进入，页面首屏渲染正常 | 检查文案和分组层次 |
| T09 | 新建提醒 | 进入编辑页、时间选择器、标题/日期/重复设置 | 真机操作 | 通过 | 编辑页可从 FAB 正常拉起并截图 | 对齐新 UI 稿，继续测保存链路 |
| T10 | 一次性提醒 | 新建后保存、列表展示、再次编辑 | 真机 + 本地数据 | 通过 | 编辑页切换为“每天”后可保存并返回提醒列表，基础保存链路通过 | 后续补显式数据核对与再次编辑验证 |
| T11 | 到点提醒 | 一次性闹钟到点后的通知、振动、全屏提醒 | 调试广播/真机日志 | 通过 | `plan-strong-test` 触发后出现 `channel_alarm_v2` 通知并拉起 `FullScreenAlarmActivity` | 继续观察华为行为 |
| T12 | 轻提醒 | `QUIET_REMINDER` 走普通通知，不走强提醒 | 调试广播/日志 | 通过 | `plan-quiet-test` 只进入 `channel_reminder_v1`，未出现全屏页日志 | 对齐生活提醒策略 |
| T13 | 稍后提醒 | 5/10/30 分钟稍后提醒是否重新入队 | 真机提醒页 + 日志 | 通过 | 5 分钟按钮触发后，`dumpsys alarm` 出现新的 `ALARM_FIRE`，时间为 `2026-06-06 15:15:47` | 后续继续补 10/30 分钟和上限逻辑 |
| T14 | 关闭提醒 | 提醒页关闭后是否正确结束本次 occurrence | 真机提醒页 + 日志 | 通过 | 点击关闭后全屏提醒页正常结束并返回主界面 | 后续补 DB 状态核对 |
| T15 | 倒计时 | 创建、暂停、继续、+1 分钟、重置 | 真机操作 | 失败 | 首页 FAB 与模板页均未提供“新建倒计时”入口；手工注入 3 分钟倒计时后，暂停/继续/+1 分钟/重置动作均正常 | 补齐创建入口，并继续评估是否需要更强运行态反馈 |
| T16 | 本地模式 | 不登录直接使用、数据可见、退出登录后回本地 | 真机操作 | 通过 | 当前真机可直接进入首页；调试日志显示 `userId=0`，本地模式工作正常 | 校验游客文案一致性 |
| T17 | 小组件 | `下一个提醒` / `今日提醒列表` 显示与刷新 | 真机桌面 | 待人工 | 当前未进入桌面添加流程 | 后续做 widget 手工验收 |
| T18 | 恢复链路 | `BOOT_COMPLETED / TIMEZONE_CHANGED / TIME_SET` 后重建调度 | 真机/受保护广播 | 待人工 | 需要重启设备或更深系统验证 | 优先做脚本化回归方案 |
| T19 | 数据安全 | DB 备份、迁移与旧数据保留 | 代码检查 + 定向验证 | 通过 | 代码检查确认启动前执行 `smartclock.db / -wal / -shm` 备份，Room 已注册 `1→2→3→4` 迁移；真机 `run-as` 可见 `.bak` 文件，当前未发现旧数据覆盖风险 | 后续补 migration 自动化测试与旧库样本回放 |
| T20 | UI 优化 | 布局拥挤、间距、状态标签、颜色层级 | 连续截图审查 | 未测 |  | 逐页继续收 UI 稿 |

## 执行记录
### Round 1
- `T01`：`assembleDebug testDebugUnitTest` 通过。
- `T02-T08`：真机页面导航回归完成，提醒、日历、模板、设置四页均可进入，无崩溃。
- `T06`：日历月/周/日视图切换通过。
- `T09`：FAB 可正常拉起新建提醒页。
- `T10`：编辑页切到“每天”后可保存并返回提醒列表，基础保存链路通过。
- `T11`：强提醒即时触发时，日志确认全屏提醒页被拉起。
- `T12`：轻提醒即时触发时，仅显示提醒通知，不拉起全屏页。
- `T13`：5 分钟稍后提醒会重新写入系统闹钟。
- `T14`：关闭动作会结束提醒页并回到主界面。
- `T16`：本地模式已验证，当前设备可不登录直接使用。
- 当前仍待继续：保存链路、稍后提醒、关闭提醒、倒计时、小组件、恢复链路、数据安全。

### Round 2
- `T15`：首页切到“倒计时”筛选后无可创建入口，右下角 FAB 仍固定进入普通提醒编辑页；模板页 8 个模板中也没有倒计时模板，创建链路失败。
- `T15`：向本地库注入 3 分钟倒计时后，真机验证了启动、暂停、继续、`+1 分钟`、重置，运行态按钮和剩余时间刷新均正常。
- `T19`：代码检查确认数据库初始化前会先备份 `db / wal / shm`，Room 已显式注册 `1→2→3→4` 迁移；真机沙箱内可见 `.bak` 备份文件。

## 当前重点
1. 先完成构建、导航、页面稳定性、提醒主链路。
2. 再回到倒计时、小组件、恢复链路和数据安全。

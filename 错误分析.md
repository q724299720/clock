# 错误分析

## 使用规则
1. 每次遇到新问题，先搜索本文件。
2. 统一按 `现象 / 根因 / 解决方案 / 下次排查顺序` 记录。
3. 同一类错误不重复试错，优先复用这里的结论。
4. 如果文件出现乱码或局部补丁持续失败，优先整文件重写，不继续硬补。

---

## 2026-06-06

### 1. Figma MCP Starter 套餐额度耗尽
- 现象
  - `create_new_file` 成功，但 `search_design_system` / `use_figma` 返回 `You've reached the Figma MCP tool call limit on the Starter plan`。
- 根因
  - Figma Starter 套餐的 MCP 调用额度已耗尽，不是代码或权限错误。
- 解决方案
  - 保留已创建的文件，等待额度恢复或升级套餐后继续。
- 下次排查顺序
  1. 先看报错里是否包含 `Starter plan` 或 `tool call limit`。
  2. 如果命中，停止继续调 Figma MCP，直接判定为额度问题。

### 2. Compose 旧版本 API 与新写法不兼容
- 现象
  - 出现 `Unresolved reference`、`Cannot access internal`、`None of the following functions can be called` 之类编译错误。
- 根因
  - 当前仓库 Compose 版本不是最新，直接照新版文档写 API 会失败。
- 解决方案
  - 回到仓库现有 API 风格。
  - 实验性 API 例如 `FlowRow` 需要显式 `@OptIn`。
- 下次排查顺序
  1. 先确认是不是 Compose 版本差异。
  2. 再决定是改写法还是补 opt-in。

### 3. Gradle Wrapper 缺失导致工程无法构建
- 现象
  - 根目录没有 `gradlew` / `gradlew.bat`，无法直接构建。
- 根因
  - 仓库缺少完整 wrapper。
- 解决方案
  - 补齐 wrapper 文件和 `gradle-wrapper.jar`。
- 下次排查顺序
  1. 先查根目录是否有 `gradlew.bat`。
  2. 没有就先补工具链，不先查业务代码。

### 4. 新 shell 不继承 JDK / Android SDK 环境变量
- 现象
  - Gradle 报 `JAVA_HOME is not set` 或 `SDK location not found`。
- 根因
  - 新开 PowerShell 时没有继承之前临时设置的 JDK / SDK 环境变量。
- 解决方案
  - 每次执行 Gradle 前显式注入 `JAVA_HOME`、`ANDROID_HOME`、`ANDROID_SDK_ROOT`。
- 下次排查顺序
  1. 先看是不是新 shell。
  2. 再补环境变量，不先怀疑 Gradle 配置。

### 5. PowerShell 不适合直接处理中间二进制流
- 现象
  - `adb exec-out` 导出截图或数据库时出现 0 字节文件、字节写入错误、转义异常。
- 根因
  - PowerShell 文本管道不适合直接处理中间二进制流。
- 解决方案
  - 优先使用 `Start-Process -RedirectStandardOutput`。
  - 导出后再校验文件大小。
- 下次排查顺序
  1. 看到 0 字节或字节流异常，先怀疑导出方式。
  2. 改用 `RedirectStandardOutput`。

### 6. adb 不能直接启动 exported=false 的内部组件
- 现象
  - 用 `adb am start` 或 `start-foreground-service` 直接触发内部组件失败。
- 根因
  - 目标组件 `exported=false`，Android 安全模型会拦截。
- 解决方案
  - 改走 App 自己的调试入口或真实 UI 链路。
- 下次排查顺序
  1. 先查 Manifest 的 `exported`。
  2. 如果是 `false`，不要继续硬起组件。

### 7. 本地模式负数闹钟 ID 被错误判定为非法
- 现象
  - `AlarmManager` 已注册，到点后系统广播送达，但 App 不继续拉起提醒。
- 根因
  - 本地未同步提醒使用负数临时 ID；提醒链路把 `alarmId <= 0` 当非法值丢掉。
- 解决方案
  - 改成只有 `alarmId == 0` 才算非法。
- 下次排查顺序
  1. 先核对提醒是不是本地负数 ID。
  2. 再查 `Receiver / Service` 是否还在判断 `<= 0`。

### 8. 前台服务未及时前台化会导致闹钟触发时崩溃
- 现象
  - 真机到点时进程被杀，日志里有 `ForegroundServiceDidNotStartInTimeException`。
- 根因
  - 提醒服务被拉起后，没有足够快地调用 `startForeground(...)`。
- 解决方案
  - 在 `AlarmAlertService.onCreate()` 里先用占位通知进入前台，再更新正式通知。
- 下次排查顺序
  1. 先查是否有前台服务超时异常。
  2. 命中后先查前台化时序，不先改铃声或震动逻辑。

### 9. 华为亮屏时不保证直接全屏，只保证 Heads-up
- 现象
  - 亮屏解锁态下常见的是顶部提醒，不一定自动全屏。
- 根因
  - Android 官方行为和华为策略都更偏向 heads-up notification。
- 解决方案
  - 锁屏/灭屏继续走 `setFullScreenIntent`。
  - 亮屏解锁以高优先级通知为主，不把 overlay 当主路径。
- 下次排查顺序
  1. 先区分亮屏还是锁屏场景。
  2. 再查系统权限、通知配置和实际链路日志。

### 10. 锁屏态下 uiautomator dump 不能作为唯一依据
- 现象
  - 日志显示全屏页已启动，但 `uiautomator dump` 仍只看到 Keyguard / SystemUI。
- 根因
  - 华为锁屏态下 XML 层级更偏向暴露 Keyguard，不代表业务页未显示。
- 解决方案
  - 锁屏态优先看 `ActivityTaskManager / WindowManager / Service` 日志。
- 下次排查顺序
  1. 先看系统日志是否显示 Activity 已启动并获得焦点。
  2. 不把 Keyguard XML 当唯一证据。

### 11. 同一 Android 工程不要并发执行多个 Gradle 任务
- 现象
  - `transform...WithAsm`、`intermediates/classes` 等路径随机报错。
- 根因
  - 多个 Gradle 进程同时写同一个 `app/build`。
- 解决方案
  - `assembleDebug`、`testDebugUnitTest`、`installDebug` 串行执行。
- 下次排查顺序
  1. 报错若落在 transform 或中间产物路径，先怀疑并发构建。
  2. 改成单进程顺序执行。

### 12. 乱码文件上连续做小补丁容易把文件补坏
- 现象
  - `apply_patch` 多次报 `Failed to find expected lines`，甚至把字符串截断。
- 根因
  - 乱码上下文导致补丁匹配非常脆弱。
- 解决方案
  - 连续两次失败后停止局部补丁，直接整文件重写。
- 下次排查顺序
  1. 先重新读取真实文件内容。
  2. 如果仍不稳定，直接重写文件。

### 13. jOOQ fetchValue 返回类型在 Kotlin 中会退化成 Any
- 现象
  - `:server:compileKotlin` 报 `Type mismatch: inferred type is Any but Long was expected`。
- 根因
  - `fetchValue(...)` 在该调用场景下不是强类型 `Long?`。
- 解决方案
  - 改成 `(fetchValue(...) as? Number)?.toLong()`。
- 下次排查顺序
  1. 看到 `fetchValue` 类型错误先怀疑 jOOQ 推断。
  2. 显式转 `Number -> Long`。

### 14. Hilt 注入自定义 Store 时 Context 必须显式标注 @ApplicationContext
- 现象
  - `:app:hiltJavaCompileDebug` 报 `Context cannot be provided without an @Provides-annotated method`。
- 根因
  - 构造注入中使用裸 `Context`，Hilt 无法判断作用域。
- 解决方案
  - 在构造函数参数上标注 `@ApplicationContext`。
- 下次排查顺序
  1. 看到 `MissingBinding` 且目标类型是 `Context`，先查限定符。
  2. 应用级 Store 默认优先 `@ApplicationContext`。

### 15. jOOQ fetchExists 不能直接接原始 SQL 字符串
- 现象
  - `:server:compileKotlin` 报 `None of the following functions can be called with the arguments supplied`，出现在 `fetchExists("SELECT ...")`。
- 根因
  - `fetchExists` 的重载不接受原始 SQL 字符串。
- 解决方案
  - 改成 `fetchOne("SELECT 1 ...") != null`，或改成 jOOQ table/condition 写法。
- 下次排查顺序
  1. 看到 `fetchExists` 重载错误时，先查是不是传了原始 SQL。
  2. 原始 SQL 场景优先改 `fetchOne(...) != null`。

### 16. Kotlin DSL 中 import 必须放在文件最顶部
- 现象
  - 修改 `build.gradle.kts` 时，把 `import` 插到 `plugins {}` 后面会导致脚本编译失败。
- 根因
  - Kotlin DSL 脚本中的 `import` 只能位于顶层最前面，不能放在语句块之后。
- 解决方案
  - `import` 统一放到文件第一行区域，再写 `plugins {}`。
- 下次排查顺序
  1. `build.gradle.kts` 改完后如果出现脚本级编译错误，先查 import 位置。
  2. 确认所有 import 都在顶部。

### 17. 前端按根路径构建时不能直接挂到 /admin 子路径
- 现象
  - 管理台静态资源在 `/admin/` 场景下容易出现 404 或路由刷新失败。
- 根因
  - Vite 默认按站点根路径 `/` 构建，资源路径和 SPA fallback 都不是 `/admin/` 语义。
- 解决方案
  - 要么直接部署到站点根路径或独立子域名根路径。
  - 要么先设置 `vite.config.ts` 的 `base: "/admin/"` 再重新构建。
- 下次排查顺序
  1. 先确认前端构建时的 base。
  2. 再确认 Nginx 的挂载路径和 `try_files` 是否一致。

### 18. 控制台读取中文 Markdown 时出现乱码，不要继续在乱码内容上做局部补丁
- 现象
  - `Get-Content` 读取某些中文 Markdown 后，终端输出乱码，局部替换很容易误判上下文。
- 根因
  - 文件编码、终端代码页和 PowerShell 输出编码不一致，导致控制台显示异常。
- 解决方案
  - 如果内容较短或需要大量替换，直接整文件重写成 UTF-8。
  - 不在乱码输出基础上继续做局部补丁。
- 下次排查顺序
  1. 先判断是文件真坏了，还是仅控制台显示乱码。
  2. 如果需要大改，直接重写文件，不再依赖乱码上下文匹配。

### 19. Linux 服务器启动 Spring Boot jar 时出现 `A JNI error has occurred`
- 现象
  - `systemctl status` 中看到 `Error: A JNI error has occurred, please check your installation`
  - 服务启动即退出，通常 `ExecStart` 没进入应用逻辑就失败。
- 根因
  - 绝大多数场景是服务器实际运行的 `java` 版本过低。
  - 当前后端 jar 是用 Java 17 构建的，如果服务器用 Java 8 或 Java 11 启动，会触发 `UnsupportedClassVersionError`，但在 `status` 里常被折叠成 `JNI error`。
- 解决方案
  - 先用 `journalctl -u smartclock-server -l --no-pager -n 80` 看完整异常。
  - 用 `java -version` 和 `readlink -f /usr/bin/java` 确认当前 systemd 实际使用的 Java。
  - 安装并切换到 Java 17 后，再重启服务。
- 下次排查顺序
  1. 看到 `JNI error` 先不要怀疑 jar 损坏，先查 Java 版本。
  2. 先看完整 journal，再看 `ExecStart` 指向的 Java 路径。

### 20. Composer 第三方依赖可能被安全审计或网络超时拦住，部署优先级不应押在 Packagist
- 现象
  - `composer install` 出现 `curl error 28` 超时，或依赖因安全 advisory 被阻止安装。
- 根因
  - 服务器网络到 Packagist 不稳定，或者依赖版本被 Composer 的安全审计策略拦截。
- 解决方案
  - 对这类小型 API 后端，优先采用零第三方依赖实现。
  - 尤其是 JWT、.env 解析这类能力，可直接用内置 PHP 实现，减少部署脆弱点。
- 下次排查顺序
  1. 先判断是不是必须依赖 Composer 包。
  2. 如果不是必须，优先改成无依赖实现，而不是反复重试下载或放宽安全策略。

### 21. 宝塔 / 共享环境里 `putenv()` 可能被禁用，不要把 .env 加载依赖建立在它上面
- 现象
  - PHP 报 `Call to undefined function ... putenv()`，应用在加载 `.env` 阶段直接中断。
- 根因
  - 某些服务器环境禁用了 `putenv()`，或者没有暴露给当前 PHP 运行方式。
- 解决方案
  - `.env` 加载只写入 `$_ENV` 和 `$_SERVER`。
  - 配置读取优先从 `$_ENV` / `$_SERVER` 取值，不依赖 `getenv()/putenv()`。
- 下次排查顺序
  1. 看到 `.env` 初始化阶段崩溃，先查是否调用了 `putenv()`。
  2. 宝塔环境优先走无 `putenv()` 方案。

### 22. 老 MySQL / MariaDB 对 `JSON` 列支持不一致，容易导致审计表单独建表失败
- 现象
  - 业务主表可用，但访问审计日志接口时报 `internal server error`。
  - 常见表现是 `users`、`alarms` 正常，`admin_audit_logs` 相关接口单独失败。
- 根因
  - 数据库版本较旧或兼容模式不足，`JSON` 列建表失败，导致导入 schema 时前面表成功、最后审计表缺失。
- 解决方案
  - 审计表的 `detail_json` 改用 `LONGTEXT`，代码层仍按字符串处理。
  - PHP 入口在 500 catch 中写 `error_log`，方便服务器上直接定位具体 SQL 异常。
- 下次排查顺序
  1. 先执行 `SHOW TABLES LIKE 'admin_audit_logs';`
  2. 如果表不存在，优先怀疑 schema 在 `JSON` 列处失败。
  3. 老库环境直接用 `LONGTEXT` 版本重建该表。

### 23. Vue 里直接用 localStorage 计算登录态不会自动触发页面切换
- 现象
  - 管理台提示“登录成功”，但页面仍停留在登录表单，不跳转到后台列表。
- 根因
  - 登录态写成 `computed(() => Boolean(localStorage token))`；`localStorage` 不是响应式数据源，写入 token 后 Vue 不会重新计算模板分支。
- 解决方案
  - 改成 `ref(Boolean(getAccessToken()))` 作为真实登录态。
  - 登录成功后显式 `loggedIn.value = true`，退出登录时显式 `loggedIn.value = false`。
  - 重新构建前端并上传新的 `dist/index.html` 和 `dist/assets/`。
- 下次排查顺序
  1. 如果提示登录成功但页面不切换，先查前端状态是不是依赖 localStorage 的非响应式读取。
  2. 再查是不是服务器上还在跑旧版静态资源。

### 24. 修改 `local.defaults.properties` 后，Gradle 可能继续使用旧的 `BuildConfig.API_BASE_URL`
- 现象
  - 根目录 `local.defaults.properties` 已改成正式域名，但重新执行一次 `assembleDebug` 后，`app/build/generated/.../BuildConfig.java` 里仍然是旧的占位地址。
- 根因
  - Gradle daemon / 增量构建状态没有及时反映这类本地配置文件变更，导致生成的 `BuildConfig` 继续沿用旧配置。
- 解决方案
  - 执行 `gradlew --stop` 后再 `clean assembleDebug`。
  - 重新检查生成的 `BuildConfig.API_BASE_URL` 是否已经切到新值。
- 下次排查顺序
  1. 如果 APK 明明重打了但仍命中旧环境地址，先看 `BuildConfig.java`。
  2. 如果 `BuildConfig` 仍是旧值，先停 daemon 再 clean 重构，不先怀疑业务代码。
